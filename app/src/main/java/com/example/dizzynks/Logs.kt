package com.example.dizzynks

import androidx.appcompat.app.AppCompatActivity

class Logs : AppCompatActivity() {
    companion object {
        var data: String = "NAME, SHAPE, SIZE, CONTROLS, LEVEL, MILISECONDS\n"
    }
}